{
    "name": "rexpump-e2e-tests",
    "version": "1.0.0",
    "description": "E2E tests for RexPump Metadata API",
    "type": "module",
    "scripts": {
        "test": "bun run test/test_rexpump_api.ts",
        "test:quick": "SKIP_RATE_LIMIT=1 bun run test/test_rexpump_api.ts",
        "deploy": "cd foundry && forge script script/Deploy.s.sol --rpc-url $RPC_URL --private-key $PRIVATE_KEY --broadcast",
        "build": "cd foundry && forge build"
    },
    "dependencies": {
        "viem": "^2.21.0"
    },
    "devDependencies": {
        "@types/bun": "latest"
    }
}